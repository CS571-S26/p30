
  # Create UI in Code

  This is a code bundle for Create UI in Code. The original project is available at https://www.figma.com/design/FSYOzK5o8dEdSMSGVj9tkF/Create-UI-in-Code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  